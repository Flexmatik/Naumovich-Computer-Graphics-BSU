# Лабораторная работа №3: Базовые растровые алгоритмы

## 📝 Описание проекта
Данное приложение разработано на языке C++ с использованием библиотек **GTKmm 3.0** и **Cairomm**. Оно предназначено для визуализации работы базовых растровых алгоритмов построения отрезков, окружностей и кривых на дискретной пиксельной сетке.

Приложение поддерживает пошаговую анимацию построения, изменение масштаба сетки и замер времени выполнения алгоритмов.

---

## 🛠️ Реализованные алгоритмы

### Базовая часть (100 баллов)
1.  **Пошаговый алгоритм (Step-by-Step):** Наивный алгоритм, основанный на уравнении прямой $y = kx + b$.
2.  **ЦДА (DDA):** Цифровой дифференциальный анализатор. Инкрементальный алгоритм с использованием вещественной арифметики.
3.  **Алгоритм Брезенхема (Отрезок):** Оптимизированный алгоритм, использующий только целочисленную арифметику и анализ ошибки.
4.  **Алгоритм Брезенхема (Окружность):** Генерация окружности с использованием 8-кратной симметрии.

### Дополнительные задания (Бонусы)
5.  **Алгоритм Кастла-Питвея (Castle-Pitteway):** Структурный/лингвистический метод построения линии.
6.  **Сглаживание Ву (Wu's Anti-aliasing):** Генерация линий с устранением ступенчатого эффекта (aliasing) через управление прозрачностью.
7.  **Кривые Безье (Quadratic):** Построение гладких кривых второго порядка.

---

## 🚀 Инструкция по сборке и запуску

### Предварительные требования
Для сборки проекта необходима ОС Linux (или WSL) и следующие пакеты:
* `g++` (с поддержкой C++17)
* `cmake`
* `libgtkmm-3.0-dev`

**Установка зависимостей (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install build-essential cmake libgtkmm-3.0-dev
```
# Сборка проекта
Выполните следующие команды в корневой директории проекта:
```bash
# 1. Создаем директорию для сборки
mkdir build
cd build

# 2. Генерируем Makefile через CMake
cmake ..

# 3. Компилируем
make
```

# Запуск
После успешной компиляции запустите приложение из папки build:

```bash
./raster_app
```